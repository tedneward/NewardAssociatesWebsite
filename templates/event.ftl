<#include "header.ftl">
	
	<#include "menu.ftl">

	<div class="page-header">
		<h1>Event: ${content.title}</h1>
        <h2>${content.location}: ${content.start} - ${content.end}</h2>
	</div>

	<p>${content.body}</p>
	
<#include "footer.ftl">
