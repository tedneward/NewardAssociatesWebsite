<#include "header.ftl">

	<#include "menu.ftl">

	<h2>${content.title}</h2>

	<a href="${content.link}"><img src="${content.rootpath}img/${content.image}" style="float:left; padding: 0px 8px 0px 8px;" /></a>
	<p>${content.body}</p>

	<a href="${content.link}">Publisher's page for the book</a>

<#include "footer.ftl">